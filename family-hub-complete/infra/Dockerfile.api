FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --omit=dev 2>/dev/null || npm install --omit=dev
COPY server.mjs auth.mjs ./
COPY sync/ ./sync/
RUN mkdir -p data
EXPOSE 3001
CMD ["node","server.mjs"]
